<Window x:Class="RockefellerFiction.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Update verfügbar"
        Width="720"
        Height="560"
        MinWidth="620"
        MinHeight="460"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BgBrush}"
        Foreground="{DynamicResource TextBrush}">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource BgBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="CaretBrush" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource MutedTextBrush}"/>
            <Setter Property="SelectionBrush" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Padding" Value="8"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,6"/>
        </Style>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Eine neue Version von RockefellerFiction ist verfügbar."
                   FontSize="18"
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource TextBrush}"
                   Margin="0,0,0,12"/>

        <TextBlock Grid.Row="1"
                   x:Name="VersionText"
                   Foreground="{DynamicResource TextBrush}"
                   Margin="0,0,0,14"/>

        <StackPanel Grid.Row="2" Margin="0,0,0,12">
            <TextBlock Text="Release:"
                       Foreground="{DynamicResource TextBrush}"
                       FontWeight="SemiBold"/>
            <TextBlock Foreground="{DynamicResource TextBrush}"
                       TextWrapping="Wrap"
                       Margin="0,3,0,0">
                <Hyperlink x:Name="ReleaseHyperlink"
                           Foreground="#66B3FF"
                           RequestNavigate="Link_RequestNavigate">
                    <Run x:Name="ReleaseUrlRun"/>
                </Hyperlink>
            </TextBlock>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,0,0,14">
            <TextBlock Text="Direkter Download der neuesten ZIP:"
                       Foreground="{DynamicResource TextBrush}"
                       FontWeight="SemiBold"/>
            <TextBlock Foreground="{DynamicResource TextBrush}"
                       TextWrapping="Wrap"
                       Margin="0,3,0,0">
                <Hyperlink x:Name="DownloadHyperlink"
                           Foreground="#66B3FF"
                           RequestNavigate="Link_RequestNavigate">
                    <Run x:Name="DownloadUrlRun"/>
                </Hyperlink>
            </TextBlock>
        </StackPanel>

        <Grid Grid.Row="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="Changelog:"
                       Foreground="{DynamicResource TextBrush}"
                       FontWeight="SemiBold"
                       Margin="0,0,0,5"/>

            <TextBox Grid.Row="1"
                     x:Name="ChangelogText"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Disabled"/>
        </Grid>

        <Button Grid.Row="5"
                Content="Schließen"
                Width="100"
                HorizontalAlignment="Right"
                Margin="0,14,0,0"
                IsDefault="True"
                Click="Close_Click"/>
    </Grid>
</Window>
